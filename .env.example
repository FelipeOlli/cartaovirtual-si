# Cartão virtual (HTML estático) — sem variáveis obrigatórias em runtime.
# O nginx escuta na porta 80 dentro do container (mapeie no EasyPanel ou no compose).

# Exemplo se no futuro houver backend ou analytics:
# PUBLIC_URL=https://cartao.sitecnologia.tec.br
